import { Typography, Box } from "@mui/material";

export default function HistoryPage() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 2 }}>
        Nuestra Historia
      </Typography>
      <Typography>
        Aquí contarás la historia de la bodega (timeline, fechas, etc.).
      </Typography>
    </Box>
  );
}
