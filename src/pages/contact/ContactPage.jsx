import { Typography, Box } from "@mui/material";

export default function ContactPage() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 2 }}>
        Contacto
      </Typography>
      <Typography>
        Aquí pondremos el formulario de contacto y el mapa de Google.
      </Typography>
    </Box>
  );
}
